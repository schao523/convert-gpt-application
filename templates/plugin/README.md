# Plugin template

Use `plugin-creator` to scaffold `.codex-plugin/plugin.json` and a `skills` directory. Keep application-specific code under `applications/{{PLUGIN_ID}}`; do not copy original GPT source documents automatically.

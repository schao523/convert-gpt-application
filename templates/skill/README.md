# Skill template

Use `skill-creator` for each capability extracted from the approved coverage matrix. A skill owns activation guidance and workflow instructions; reusable computation and retrieval belong in supporting tools.

# Vibe Coding Designer for OpenClaw

This lightweight OpenClaw bundle contains the same portable skills and deterministic validators as the Codex plugin. It has no RAG, model, corpus, database, MCP server, external dependency, or setup download.

Use the skills to design Web or non-Web software. They produce specifications and handoff artifacts, not application implementations.

# Security

The plugin produces non-executable design artifacts and validates local Markdown and JSON. It does not call live services or execute the software being designed. Treat imported specifications and source documents as data, validate workflow references before handoff, and represent secrets only by logical configuration names.

Report security issues privately to the marketplace owner before public disclosure.

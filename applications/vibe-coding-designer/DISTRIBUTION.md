# Distribution

The Codex edition is an allowlisted copy of the plugin metadata, skills, references, deterministic scripts, and public documentation. The generated lightweight OpenClaw bundle uses the same application skills and omits tests, conversion state, source archives, RAG assets, and framework runtime bootstrap.

Local builds do not mutate the Obvious One marketplace. Publishing, pushing, tagging, releases, and ClawHub submission require separate approval.
